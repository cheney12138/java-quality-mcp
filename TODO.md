# TODO

## 方向 2：Flavor 相关性过滤

**背景**：`get_style_context` 目前返回最近 5 条 flavor 样本，与当前任务无关的样本会占位挤掉相关样本。

**讨论结论**：暂搁置。
- intent 由 Claude 自动填入，主观性强，词汇不一定与样本 tags 对齐
- 命中率难以量化，改完不知道是变好还是变差
- 现有样本少时问题不明显，等样本积累到 20+ 条且出现明显"挤掉"问题时再设计

**可能的方向**：限制每个 tag 下最多存 N 条（而非靠文本相似度排序），简单可预期。

---

## 方向 3：Design hints 扩展

**背景**：目前只检测 3 个结构模式（else-if chain、instanceof chain、顺序 if-return），覆盖面有限。

**待验证**：等 `/quality-report` 积累几周数据，看 `design_hints` 实际触发频率和 Claude 采纳情况，再决定是否扩展。

**下一个候选**：`switch` 分支 ≥ 5 case → Strategy 模式（tree-sitter 直接查 `switch_block_statement_group` 数量，实现成本低）。

**暂不做的原因**：hint 太多会变噪音，需先验证现有 3 个的实际价值。

---

## 方向 4：项目级规则覆盖

**背景**：不同项目有不同规范诉求（如老项目方法行数阈值较宽松），全局一套规则无法满足。

**待验证场景**：收集真实使用中的定制化需求（如 methodLength 阈值敏感的老项目），再归纳设计。

**设计原则**：
- schema 语义化，用户只需关心业务语义（`methodLengthMax: 120`），不暴露 Checkstyle/PMD 规则名
- 底层映射由 MCP 内部维护
- 参考 `.eslintrc` / `.prettierrc` 模式，配置文件跟随项目代码提交

**暂不做的原因**：schema 内容宽泛，需真实场景汇总后再设计，避免过度设计。

---

## 方向 5：与 GitNexus 打通

**背景**：GitNexus 已有完整的代码图谱（调用链、模块边界、热点方法），是静态分析拿不到的代码事实。

**设想**：`validate_code` 或新增工具结合 GitNexus 上下文，将设计建议从"代码结构层面"升维到"系统影响层面"。

**示例**：
- 当前：`validate() 有 5 个分支 → 建议责任链`
- 结合图谱：`validate() 在支付主链路上被 12 处调用，是热点方法，修改风险高，建议用责任链降低耦合`

**前置条件**：需要 GitNexus MCP 工具的调用协议对齐，以及确定数据传递方式。
